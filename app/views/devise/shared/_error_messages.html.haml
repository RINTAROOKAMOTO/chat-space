- if resource.errors.any?
  #error_explanation
    %h2
      = I18n.t("errors.comments.not_saved",                 |
        count: resource.errors.count,                       |
        resource: resource.class.model_name.human.downcase) |
    %ul
      - resource.errors.full_comments.each do |comment|
        %li= comment
